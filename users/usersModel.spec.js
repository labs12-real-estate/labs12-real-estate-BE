const db = require('../database/dbConfig.js');
const Users = require('./usersModel.js');

describe('', () => {
  beforeEach(async () => {
    await db('').truncate();
  });

  describe('', () => {
    it('', async () => {
    });
  });
});

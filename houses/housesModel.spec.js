const db = require('../database/dbConfig.js');
const Houses = require('./housesModel.js');

describe('', () => {
  beforeEach(async () => {
    await db('').truncate();
  });

  describe('', () => {
    it('', async () => {
    });
  });
});
